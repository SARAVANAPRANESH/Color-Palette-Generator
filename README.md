# Color Palette Generator

Generates a random 5-color palette with click-to-copy hex codes.

## Tech Stack
- HTML
- CSS
- JavaScript (vanilla, no frameworks/libraries)

## How to Run
1. Clone this repository
2. Open `index.html` in your browser

That is it -- no build step, no dependencies.
